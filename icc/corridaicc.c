#include <stdio.h>
int main(void) {
    int n;
    scanf("%d", &n);
    printf("%d\n", (n-4)/2); // 4 é o número de curvas 100% seguras. Tirando as 4, para continuar o trajeto sempre serão adicionadas mais 2 curvas e entre essas 1 é sempre perigosa.
    return (0);
}